Лабораторная работа №8. Введение в React + JSX

![alt text](image.png)

- Что такое компонент, пропсы и состояние.

1) Компонент — это независимая, переиспользуемая часть интерфейса (функция или класс), которая описывает, что показывать на экране.

2) Пропсы (props) — это данные, которые компонент получает от родителя. Они доступны только для чтения.

3) Состояние (state) — это изменяемые данные внутри компонента, которые определяют его внешний вид и поведение в текущий момент.

- Какую часть кода помог написать ИИ.

- ИИ написал базовое React приложение которе позволяет создавать задачи и удалять их.

-Что вы поняли о компонентном подходе

- В React мы описываем, как должен выглядеть интерфейс для каждого возможного состояния данных. Когда состояние меняется, React автоматически обновляет интерфейс, чтобы он соответствовал новому состоянию. Нам не нужно вручную управлять элементами DOM — React делает это за нас.
